import React from 'react';
import { StyleSheet, Text, View, Image } from 'react-native';

const Premiere = ({ data }) => {
  return <Text style={styles.comingSoon}>Premiere...</Text>;
};

const styles = StyleSheet.create({
  comingSoon: {
    flex: 1,
  },
});

export default Premiere;
